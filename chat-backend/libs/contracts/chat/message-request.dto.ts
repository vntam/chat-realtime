import { ApiProperty } from '@nestjs/swagger';

export class AcceptMessageRequestDto {
  // No body needed, conversationId from params
}

export class DeclineMessageRequestDto {
  // No body needed, conversationId from params
}
