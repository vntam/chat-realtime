# Production Environment Variables for AWS S3 Deployment
# Render URLs (correct service names with suffixes)

# API Gateway URL (Single Entry Point for REST API)
VITE_API_GATEWAY_URL=https://chat-api-gateway-ahun.onrender.com

# WebSocket URLs (Direct connection to services - bypass Gateway)
# NOTE: Notification Service has suffix -bvhb!
VITE_CHAT_WS_URL=https://chat-chat-service.onrender.com
VITE_NOTIFICATION_WS_URL=https://chat-notification-service-bvhb.onrender.com

# Architecture reminder:
#   - REST API (HTTP) → API Gateway → Services
#   - WebSocket (WS) → Direct to Services (bypass Gateway)
