# Production Environment Variables for AWS S3 Deployment
# Copy this file to .env.production and update with your actual backend URLs

# API Gateway URL (Single Entry Point for REST API)
# Thay bằng URL thật của API Gateway trên Render/Railway/EC2
VITE_API_GATEWAY_URL=https://your-api-gateway.onrender.com

# WebSocket URLs (Direct connection to services - bypass Gateway)
# WSS là WebSocket Secure (version HTTPS của WebSocket)
VITE_CHAT_WS_URL=wss://your-chat-service.onrender.com
VITE_NOTIFICATION_WS_URL=wss://your-notification-service.onrender.com

# Example if using Railway.app:
# VITE_API_GATEWAY_URL=https://your-api-gateway.up.railway.app
# VITE_CHAT_WS_URL=wss://your-chat-service.up.railway.app
# VITE_NOTIFICATION_WS_URL=wss://your-notification-service.up.railway.app

# Example if using AWS EC2/Elastic Beanstalk:
# VITE_API_GATEWAY_URL=https://api.yourdomain.com
# VITE_CHAT_WS_URL=wss://chat.yourdomain.com
# VITE_NOTIFICATION_WS_URL=wss://notification.yourdomain.com

# Architecture reminder:
#   - REST API (HTTP) → API Gateway → Services
#   - WebSocket (WS) → Direct to Services (bypass Gateway)
